describe('TravelService', function() {

  beforeEach(module('travelModule'));

  it('should ...', inject(function(travelService) {

	//expect(sampleService.doSomething()).toEqual('something');

  }));

});